works the same as a Map constructor, takes an array of [key, value] pairs to construct an object

```ts
const a = Object.fromEntries([["a", 1], ["b", 2], ["c", 3]]);

print(a); // {a: 1, b: 2, c: 3}
```
